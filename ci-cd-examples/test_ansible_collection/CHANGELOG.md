# Changelog

All notable changes to `kerrys_learning_lab.base` are documented here.
This project adheres to [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added
- Collection scaffold: `galaxy.yml`, `meta/runtime.yml`, CI (lint + build),
  README with development workflow, and supporting repo config. No roles yet —
  roles are migrated in a follow-up (see ADR 0001 in `devsecops/ansible`).

## [0.1.0] - 2026-06-13

### Added
- Initial empty scaffold.
